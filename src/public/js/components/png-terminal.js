import { sectionEl } from './core.js';

/* <png-terminal title> — presentational (DSD); console lines slot in and are
   styled via .terminal-body ::slotted(p) / ::slotted(.out). Hydrate only. */
sectionEl('png-terminal');
